

class SVD():
    def _init_(self, data, rank, learning_rate, reg):
        self.ratings = data
        self.num_users = data.shape(axis=0)
        self.num_features = data.shape(axis=1)
        
        
    
    
    def compute_SVD(self, ratings):
        
        
    
    def predict(self, userindex, itemindex):
        